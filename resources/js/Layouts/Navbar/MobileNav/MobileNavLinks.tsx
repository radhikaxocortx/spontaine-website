interface Properties {
  isVisible: boolean
  setIsVisible: React.Dispatch<React.SetStateAction<boolean>>
}

const MobileNavLinks = ({ isVisible, setIsVisible }: Properties) => {
  return <></>
}

export default MobileNavLinks
